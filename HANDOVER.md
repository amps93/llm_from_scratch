# LLM From Scratch 핸드오버

이 문서는 회사와 집에서 Codex 작업을 이어갈 때 읽는 **현재 상태 기록**이다. 목표와 설계 근거는 [PLAN.md](PLAN.md), 프로젝트 규칙은 [AGENTS.md](AGENTS.md)에 있다. 새 세션에서는 먼저 Git 상태와 이 문서가 실제 파일 상태에 맞는지 확인한다.

## 현재 상태

- 단계: **설계 완료, 구현 시작 전**
- 완료: RTX 4060 Ti 16GB 기준의 PyTorch 스크래치 학습 계획 작성; K2/AI Hub 자료의 역할 구분; GitHub 저장소 생성; 프로젝트 README 및 Git 제외 규칙 작성
- 구현: 아직 없음
- 학습 데이터: 아직 다운로드·정제하지 않음
- 학습 및 평가 결과: 아직 없음
- 원격 저장소: https://github.com/amps93/llm_from_scratch
- 마지막 확인: 2026-09-18, 회사에서 대화 중. 현재 실행 환경의 GPU·CUDA·PyTorch는 **미확인**. 이 저장소의 Git 작성자는 로컬 설정으로 `amps93`에 고정됨

## 다음 작업

1. 작업 중인 PC의 GPU, VRAM, CUDA, Python, PyTorch, 디스크 여유를 확인하고 결과를 아래 환경 표에 기록한다.
2. `PLAN.md`의 **단계 1: 최소 PyTorch 모델**을 구현한다. 작은 입력에서 causal mask, attention 출력, loss, gradient를 확인하고 작은 배치를 과적합시킨다.
3. 첫 구현과 검증 결과를 이 문서에 남긴다. 데이터 다운로드나 대형 학습은 그다음 단계다.

## 환경별 기록

| 항목 | 회사 | 집 |
|---|---|---|
| OS / 아키텍처 | 미확인 | 미확인 |
| GPU / VRAM | 미확인 | 사용자가 RTX 4060 Ti 16GB 환경을 언급함. 실제 장착 PC는 미확인 |
| Python / PyTorch / CUDA | 미확인 | 미확인 |
| 데이터 경로 | 미확인 | 미확인 |
| 체크포인트 경로 | 미확인 | 미확인 |
| 최근 실행 결과 | 없음 | 없음 |

공유 가능한 정보만 적는다. 계정, 토큰, 키, 개인 데이터, 회사 내부 경로는 기록하지 않는다.

## 세션 시작 절차

1. `git status --short --branch`로 로컬 변경을 확인한다.
2. 원격 변경을 확인한 뒤 안전하게 최신 `main`을 반영한다. 로컬 변경이 있으면 먼저 충돌 가능성을 검토한다.
3. `AGENTS.md` → `HANDOVER.md` → 필요한 `PLAN.md` 절을 읽고, 이번 세션의 작업을 정한다.
4. 현재 PC에 필요한 데이터·환경이 없으면 구현과 작은 CPU 테스트처럼 가능한 작업부터 진행한다.

## 세션 종료 시 갱신 항목

- 실제 완료한 작업과 바뀐 파일
- 실행한 검증 명령과 결과, 재현되지 않은 문제
- 마지막 Git 커밋 및 원격 반영 여부
- 다음 한 가지 구체적인 작업
- 회사/집 중 이번에 확인한 환경 차이

작업을 GitHub에 푸시했다면 다음 PC에서 원격 변경을 가져오면 된다. 푸시하지 못했다면 이 문서에 **로컬에만 있는 커밋이나 변경**을 명시한다. Git에 포함되지 않는 원본 데이터와 체크포인트는 필요한 경우 별도 이동 방법을 기록하되, 비밀값이나 제한된 데이터를 저장소에 넣지 않는다.

## 새 Codex 세션에 보낼 짧은 요청

> 이 저장소의 `AGENTS.md`, `HANDOVER.md`, `PLAN.md`를 읽고 실제 Git 상태와 대조해 주세요. `HANDOVER.md`의 다음 작업부터 이어서 진행하고, 작업을 마치면 검증 결과와 다음 단계를 핸드오버 문서에 갱신해 주세요. 현재 PC의 환경을 다른 PC와 동일하다고 가정하지 마세요.

## 작업 이력

| 날짜 | 작업 | 결과 |
|---|---|---|
| 2026-09-18 | 스크래치 학습 계획 작성·보완 | `PLAN.md` 작성, `main` 첫 커밋 `27844e8` 푸시 |
| 2026-09-18 | 회사/집 이동용 핸드오버 준비 | 이 문서와 `AGENTS.md` 추가 |
| 2026-09-18 | 저장소 시작 문서·제외 규칙 준비 | `README.md`, `.gitignore` 작성; 구현은 아직 시작 전 |
| 2026-09-18 | Git 작성자 정보 정정 | 기존 4개 커밋의 작성자·커미터를 `amps93`으로 재작성하고 `main`에 반영. 이전 커밋 ID는 더 이상 현재 `main`의 ID가 아님 |
