# 프로젝트 작업 규칙

- `.env` 파일을 직접 열어보지 않는다.
- 함수를 작성할 때 기능과 사용법을 알 수 있도록 한글 Description 또는 docstring을 작성한다.
- 새 작업을 시작할 때 `PLAN.md`, `HANDOVER.md`, `git status`, `git log -1`, 원격 브랜치 상태를 확인한다. 문서의 과거 기록보다 실제 파일과 Git 상태를 우선한다.
- 사용자가 다른 장소에서 작업을 이어갈 수 있도록, 의미 있는 작업이 끝날 때 `HANDOVER.md`의 현재 상태·검증 결과·다음 작업·환경별 차이·주의점을 갱신한다. 확인하지 않은 사항은 추측하지 않고 `미확인`이라고 적는다.
- 회사와 집의 경로, GPU, CUDA, 데이터 위치, 인증 상태가 같다고 가정하지 않는다. 절대 경로와 비밀값은 커밋하지 않는다.
- 사용자의 목표는 PyTorch로 모델 구조와 학습 과정을 직접 구현하고 무작위 초기화부터 학습하는 것이다. K2 Horizon은 공개 데이터·아키텍처·학습 방법의 참고 자료이며, 기존 모델 가중치는 사용하지 않는다.
- 원본 학습 데이터, 체크포인트, 인증 정보는 Git에 넣지 않는다. 원격 변경과 로컬 변경이 충돌하면 덮어쓰지 말고 차이를 확인한다.
